package qb_test

import "testing"

func TestAPI(t *testing.T) {
	/*
		// or it would return the query builder itself which has both the String() and Vars()
		// which could then be passed into exec or query etc...
		// orm.Query and QueryRow accordingly

		str := qb.CreateTable("users", func (t *TableBuilder) {
			t.Char("hello", )
			t.Varchar()
		})

	*/
}
